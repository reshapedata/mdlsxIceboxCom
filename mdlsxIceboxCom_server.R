#完成日期组件注册
mdlsxIceboxServer::salesOrderServer(input = input,output = output,session = session,dms_token = dms_token)

mdlsxIceboxServer::salesOrder_shopServer(input = input,output = output,session = session,dms_token = dms_token)

mdlsxIceboxServer::returnOrderServer(input = input,output = output,session = session,dms_token = dms_token)
mdlsxIceboxServer::eigenvaluesServer(input = input,output = output,session = session,dms_token = dms_token)
mdlsxIceboxServer::FestivalShopServer(input = input,output = output,session = session,dms_token = dms_token)
mdlsxIceboxServer::rushDateServer(input = input,output = output,session = session,dms_token = dms_token)
mdlsxIceboxServer::NationalMacroDataServer(input = input,output = output,session = session,dms_token = dms_token)
  

